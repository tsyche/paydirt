# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
build/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# Expo / React Native
.expo/
.expo-shared/
*.apk
*.aab
web-build/
# iOS native layer — generated by `just prebuild`, not committed (no custom native code yet)
apps/mobile/ios/
# android/ is committed — bare Expo workflow with custom Kotlin native code.
# Build artifacts excluded by apps/mobile/android/.gitignore
# Dropped into repo root by expo prebuild — belongs in apps/mobile/ only
/tsconfig.json

# Env / secrets
.env
.env.local
.env.*.local
*.env
config.yaml

# PocketBase — binary + runtime data (never commit these)
pocketbase/pocketbase
pocketbase/pb_data/
pocketbase/pb_static/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# Test / coverage
coverage/
.nyc_output/
test-results/
playwright-report/
blob-report/

# Audit marker (local only)
.last-audit
.audit-log
