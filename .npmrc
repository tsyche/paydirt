# React Native / Expo's Metro bundler assumes a flat node_modules layout.
# pnpm's default symlinked layout breaks resolution of transitive deps like
# @babel/runtime and expo-modules-core, so use a hoisted layout for this repo.
node-linker=hoisted
